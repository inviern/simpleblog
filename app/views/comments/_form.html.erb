<%= form_for [@comment.post, @comment] do |f| %>
    <%= render partial: 'shared/error_messages', locals: { object: @comment } %>

    <%= f.label :text %><br>
    <%= f.text_area :text %><br>
    <br>
    <%= f.submit %>
<% end %>