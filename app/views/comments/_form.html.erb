<%= form_for :comment, url: blog_post_path(@post.author.name, @post) do |f| %>
    <%= render partial: 'shared/error_messages', locals: { object: @comment } %>

    <%= f.label :text %><br>
    <%= f.text_area :text %><br>
    <br>
    <%= f.submit %>
<% end %>